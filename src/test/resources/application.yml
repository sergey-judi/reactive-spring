spring:
  data:
    mongodb:
      port: 27017
      host: localhost

service:
  data-file: data.txt
  delay:
    enabled: false
    duration: 1000ms

logging:
  level:
    tech.pm.controller: off
    org.mongodb.driver: off
